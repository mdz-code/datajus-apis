name: Datajus API Continous Integration

on:
  push:
    branches: [ main ]

jobs:
  test_code:
    runs-on: ubuntu-latest
    steps:
      - use: actions/checkout@v2
      - use: actions/setup-node@v1
        with:
          node-version: 14
      - run: npm ci
      - run: npm run start